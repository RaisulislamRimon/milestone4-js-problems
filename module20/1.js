// Practice Problem 1
// Write a function that will take hour as the input parameter and will
// convert it into minutes and will return the result in minutes.

function convertHourToMinutes(hour) {
  return hour * 60;
}

console.log(convertHourToMinutes(1)); // 60
console.log(convertHourToMinutes(2)); // 120
